This is a sandbox, where I have fun with ansible and docker. Weird things may or may not happen.
To run just do:

```sh
vagrant up node-1
# create swarm_keys.yml as the messages say
vagrant up "/node-[2-5]/"
```

Now you have a docker swarm that is up and running.
